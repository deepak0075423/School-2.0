<div class="page-header">
    <div>
        <h1>✅ Class Test Validation</h1>
        <p>Approve or reject class tests submitted by subject teachers in your section.</p>
    </div>
</div>

<% if (!tests || tests.length === 0) { %>
<div class="card">
    <div class="empty-state">
        <div class="empty-icon">✅</div>
        <h3>No Class Tests Found</h3>
        <p>No class tests are assigned to your section yet, or none have been submitted for approval.</p>
    </div>
</div>
<% } else { %>
<div class="card">
    <div class="card-header">
        <div class="card-title">Class Tests (<%= tests.length %>)</div>
    </div>
    <div class="table-wrapper">
        <table>
            <thead>
                <tr>
                    <th>Test Title</th>
                    <th>Subject</th>
                    <th>Section</th>
                    <th>Teacher</th>
                    <th>Date</th>
                    <th style="text-align:center;">Max Marks</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% const statusBadge = { SUBMITTED:'badge-blue', REJECTED:'badge-red', REOPENED:'badge-orange', FINAL_APPROVED:'badge-green' }; %>
                <% tests.forEach(test => { %>
                <tr>
                    <td class="fw-semibold"><%= test.title %></td>
                    <td><%= test.subject?.subjectName %></td>
                    <td><%= test.section?.class?.className %> – <%= test.section?.sectionName %></td>
                    <td><%= test.createdBy?.name %></td>
                    <td style="font-size:13px; color:var(--text-muted);"><%= new Date(test.testDate).toLocaleDateString('en-IN') %></td>
                    <td style="text-align:center;"><%= test.maxMarks %></td>
                    <td><span class="badge <%= statusBadge[test.status] || 'badge-orange' %>"><%= test.status.replace(/_/g,' ') %></span></td>
                    <td>
                        <a href="/teacher/results/class-test-validation/<%= test._id %>" class="btn btn-sm <%= test.status === 'FINAL_APPROVED' ? 'btn-secondary' : 'btn-primary' %>">
                            <%= test.status === 'FINAL_APPROVED' ? 'View' : 'Review' %>
                        </a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</div>
<% } %>
